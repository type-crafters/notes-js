/**
 * The maximum drag distance from the dragged element that the 
 * application will assume retains the drag's original intention.
 * @constant
 * @type {number}
 */
export const MAX_OVERDRAG_X_PX = 200;